import React from 'react';

const Nav = () => {
  return (
    <div className="nav-bg">
    <nav className="navegacion-principal contenedor">
    <a href="#"> Inicio</a>
    <a href="#"> Sobre Mi</a>
    <a href="#"> Contacto</a>
    </nav>
    </div>
  );
};

export default Nav;